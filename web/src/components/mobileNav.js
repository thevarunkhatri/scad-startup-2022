import React from "react";
import { Link } from "gatsby"

import LogoCircle from "../assets/svg/logo.svg"

import "../styles/nav.scss";

const MobileNav = () => {

    return(
        <nav className="mobileNav">
            <div clas></div>
            <div></div>
        </nav>
    );
};

export default MobileNav;
